# server {
#     listen 80;
#     server_name lialabcorretor.com;

#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }

#     # For all other requests, redirect to HTTPS.
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
    
#     server_name lialabcorretor.com; 

#     ssl_certificate /etc/letsencrypt/live/lialabcorretor.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/lialabcorretor.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Referrer-Policy "no-referrer-when-downgrade" always;
#     add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; font-src 'self'; object-src 'none'; frame-ancestors 'self';" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

#     gzip on;
#     gzip_vary on;
#     gzip_proxied any;
#     gzip_comp_level 6;
#     gzip_types text/plain text/css text/xml application/json application/javascript application/xml+rss application/atom+xml image/svg+xml;
#     gzip_min_length 256;

#     location /static/ {
#         alias /app/static/;
#         expires 1y;
#         add_header Cache-Control "public";
#     }

#     location /media/ {
#         alias /app/media/;
#         expires 1y;
#         add_header Cache-Control "public";
#     }

#     location ~ ^/(api|admin)/ {
#         proxy_pass http://backend:8000;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header Host $host;
#     }

#     location / {
#         proxy_pass http://frontend:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
    
#     error_page 500 502 503 504 /50x.html;
#     location = /50x.html {
#         root /usr/share/nginx/html;
#     }
# }

# --- TEMPORARY CONFIGURATION FOR CERTBOT VALIDATION ---
server {
    listen 80;
    server_name lialabcorretor.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}